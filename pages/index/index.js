Page({
    data: {},
    onLoad(query) {
        // 页面加载
        // console.info(`Page onLoad with query: ${JSON.stringify(query)}`);
    },
    onReady() {
        // 页面加载完成
    },
    onShow() {
        // 页面显示
    },

    onGetFromComponents(event) {
        console.log(event)
    }
});
